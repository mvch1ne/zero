# Development Log

TODO

- Next Prompt after we resume:
  Here's the previous prompt before we were cut off.
  Block the computation it so that it can only move forward and compute if I have set two things:
- The first movement (sprint start - you will pick a frame, and I can override it). Create a marker on the scrubber and on the viewport at the exact frame you predict for this start point (think of it like when the gun went). Then I can use the button you are about to create (there's actually one there in the control panel with its own stuff [startFrame, onSetStartFrame, etc]). Just move it up and allow me to use it to override the start frame
- The start point or start line (this will be the reference for all displacements). The current code is doing the velocity calculations right, I think. It just needs the start line or start point to start from there. Now allow the user to draw the start line so that you can mark which frame the CoM passed it as displacement= 0. we should be able to work with a start line that is diagonal. just find the point at which the center of mass crosses it in that is the displacement = 0 mark.
  Also when you're done remove the CoM horizontal speed from the the telemetry summary since it's still using instantaneous values

Continue. For the start frame, even if you set it, mandate that the user set it (reconfirm) before we move forward. Also once, we cross the start line, displacement shouldn't be negative so velocity shouldn't be negative. Actually let's only compute velocity from displacement equals zero - anything before that is meaningless. But right now it showing negative because it isn't clear what the start line is and what the sprint start frame is. You know what you're supposed to do but simplify it so the user just has to set the few things.

IMPORTANT: I should probably account the velocity calculations for the average sprint reaction time in the velocity calculations. So that the velocity we will compute will be realistic. What are your thoughts? Because if Bolt is getting to 10m in 1.83 with reaction time (RT) accounted for and I'm getting there in 1.83 without RT accounted, the velocity measurement will say we are the same but technically I am well behind him. So pick a baseline reaction time (150ms?) and apply it to the first timepoint (just the first timepoint right?) when doing the calculations? That will affect acceleration too because of the elapsed time but the displacement technically doesn't change. So I guess we could say that this new elapsed time will be current time + estimated reaction time. Be sure to display that we have padded for RT in the CoM panel. And even allow me to choose what the preset reaction time should be and update all calculations if it changes. What are your thoughts? OH VERY IMPORTANT: I SHOULD BE ABLE TO TURN THE REACTION TIME ADDITION OFF BECAUSE IF WE'RE DOING THE TIMING FOR A FLYING SPRINT, THERE'S NO REACTION TIME INVOLVED. IN FACT, LET'S THINK DEEPER ABOUT THE VELOCITY WE WANT FROM A FLYING SPRINT TEST. DOES THE PREVIOUS COMPUTATION LOGIC HOLD? OR CHANGE? BECAUSE IF WE ASSUME DISPLACEMENT STARTS AT ZERO WHEN ENTERING THE ZONE, THE VALUES WE GET WILL BE LOWER THAN WHAT WE'RE ACTUALLY MOVING AT. I think for flying sprints, we don't track compute velocity based on displacement per day but rather based on the fly distance and time between the entering and leaving of the fly zone. TO DISTINGUISH, LET'S CREATE DIFFERENT TIMING MODES -> STATIC START, AND FLYING START. Put them in different modes?

## Static Start

What we discussed previously. Needs a start frame and a start line (might be drawn in a possibly diagonal way depending on camera placement). We start timing at start frame and pad on a reaction time. We compute displacement from the point at which the CoM breaks the start line and from there velocity at every frame is current displacement / (elapsed time [which includes the preset reaction time for just the first frame and continues with the normal time step over frame?]). Acceleration will then be computed velocity / elapsed time (will have to account for the reaction time in the first one and build on that will normal frame time increments for all subsequent frames, right?)

## Flying Start

Set just the start and end points of the flying zone and let the timer start when the CoM hits the start (have the redundancy to set the start frame manually as backup) and let the timer stop when the CoM hits the end of the zone (again, have a redundancy). We then report the fly time for every 10m segment [default fly distance partition] and just compute the fly velocity [which is fly distance / fly time]. Where fly time is the difference between time when we crossed start and time when we crossed finish. Or wait, we could just count the number of frames between the two and use the time per frame (based on accurate frame rate) to compute, right? For the flying sprints, the focus is on the fly time and the average velocity between the zones (the formula above). No need to compute displacement and elapsed time in the build up zone before the fly. We may not even have been able to properly compute displacement because the only field in view will probably be just the fly zone a a little margin on either side. So basically by way of example if I enter the start frame at time 0.1 seconds and I leave at 0.9 seconds that means that I spent as 0.8 seconds in the fly zone. If the fly zone is 10 m long that means that I did 10 meters (m)/ 0.9 - 0.1 which is return divided by 0.8 which means the live velocity is 12.5 m/s, right? now it's likely that we might end up doing multiple flies zones back to back to back to 10, 20, 30 lose cases allow me to be able to get the fly velocity and fly times for each 10 m segment and for the total thing. and I know I keep saying 10 m for the fly segment unless add a sitting while we can update to use shorter distance for greater accuracy so something like 5 m, 1 m whatever let's just set it and then be able to use it to tweak the settings.

I think I'm beginning to lean towards the idea of having a general mode where you can do whatever you want to have full flexibility (basically what we have now). Then having a static starts mode where everything is aligned with the requirements for the static stuff. And then the fly mode which will have everything that we're talking about the flying sprints. But then the idea is that the general should still be able to do everything that the static start and the flying modes can do. Only difference is that the general mode is sort of left open but then those other ones you have to follow the procedures required. I need you to figure out how we're going to take that in to the viewport and telemetry and make it work seamlessly.

There are quite a few issues but let's try something new. You know what my requirements are. These are the most important things. Work with them and figure out a way to make the system intuitive to use but still fit what I'm asking for. Here goes a summary of the important things:

- Fly mode is requires two primary annotations: a start point and end point of the fly zone. What I'm looking for here is the fly time (take taken for CoM to get from entry to exit of the zone), fly speed (the fly distance [will be marked from the calibration] divided by the time between the CoM hitting the entry and exit points of the zones)
- Static mode is for block starts and standing starts. Here, there's a start line and we begin with our CoM (and entire body) behind the line. What we need to do here is detect when the first movement happens (you will suggest a frame and I will approve or override it). It is at this frame that the timing starts. However, it will take some time to get the CoM from its initial point to cross the starting line. We don't care about any displacement, velocity or acceleration before it crosses the start line. What we want to do is start measuring those values right from when it crosses the start line. To do so we need to detect the starting line and the frame at which the CoM crosses it. We can borrow the start zone idea from the flying sprint and setup a vertical post (using the same way the flying sprint does it) so that we automatically begin computing the above-mentioned values when the CoM crosses this point. From there we can just carry on as normal. This eliminates the need for a general mode - however we can add the option of the reaction time so that the computed velocity and acceleration values account for what the values would look like if the user had a certain reaction time. We set set the baseline to 150ms and let the user remove it or modify it if they see fit - recompute everything if anything changes. If you have any further questions, revisit the old instructions I gave you or better yet, ask me. Make this as simple and intuitive as possible for the user

Sometimes, especially when working with low frame rate videos, it may happent that there is no frame where the CoM coincides directly with the start or finish marker, in either of the modes - what would you do then?

## Next Steps

- Find what unit of measurement for the angular measures is more appropriate for biomechanical analysis. What values do researchers like Ken Clarke and Peter Weyand find in their papers - especially the 'whip from the hip' paper about thigh angular velocity?
- The first ground contact won't have a stride value. I think we can get one by allowing it to be annotated using the start position as a reference (duh!)
- Increase the font size in the telemetry section
- Work towards body view looking more like Three.js human model.
- 3D mode is a must. Alternatives to Three.js? Just to make sure there isn't a simpler but more appropriate tool (or we can't build one quickly) before I go forward with it.
- When using the export option, let the draw box disappear after I close the panel.
- When done, create desktop version (Electron.js?) so that I don't have to upload anything. Find a way to run the application on the desktop and run the Python server on the laptop as well. Will have to figure out how to manage both seamlessly (web sockets)?

## Backend

- Deploy to Fly.io because free tier is more generous and doesn't hibernate? Regardless can write code to warm up the server
-

## Documentation

- Need to find a way to get the Test Driven Development stuff done.
- Add a help section where we'll write some guides to help users. Will record a demo video and post on YouTube and link here. Will provide a download sample so users can test the platform without having their own sprint video. Can use modals to guide the user on what to do. Allow the user to turn off the modal for subsequent visits and store that in local storage but have the option to turn it back on so that it shows up every time they open the application.
- Use Claude Code to scan all the files and write documentation for every aspect of the codebase (especially the math parts. Let's make the equations be done in LaTEX). Let it explain the design decisions and everything. Then host the docs somewhere and link to it in the README.md. Claude can probably help me set that up.
- Speaking of README.md. Let Claude Code scan the entire codebase and write a really good one (for the entire project and also for the frontend and backend)
- Go to Community Standards (https://github.com/mvch1ne/sprintlab/community) and create these things so that the project is up to standard. Ask Claude Code for the things I can do that will make my project stand out.
- Work on my GitHub profile's README.md to make it better.
